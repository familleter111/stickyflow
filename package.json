{
  "name": "mdflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MDFlow - notes adhésives modernes",
  "author": "MDFlow",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "server": "node server/server.mjs",
    "dev:full": "concurrently -k \"npm run server\" \"vite\"",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "electron": "electron .",
    "electron:dev": "concurrently -k \"vite\" \"wait-on tcp:5173 && electron .\"",
    "pack": "npm run build && electron-packager . MDFlow --platform=win32 --arch=x64 --out=release --overwrite --prune=true --ignore=\"/src$\" --ignore=\"/release\" --ignore=\"/\\.\" --ignore=\"tsconfig\" --ignore=\"vite\\.config\" --ignore=\"tailwind\\.config\" --ignore=\"postcss\\.config\"",
    "dist": "npm run build && electron-builder --win"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "lucide-react": "^0.456.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.4",
    "zustand": "^5.0.1"
  },
  "devDependencies": {
    "@electron/packager": "^18.4.4",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.1.0",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "wait-on": "^8.0.1"
  },
  "build": {
    "appId": "com.mdflow.app",
    "productName": "MDFlow",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "MDFlow"
    },
    "portable": {
      "artifactName": "MDFlow-${version}-portable.exe"
    }
  }
}
